# Gemfile.ci — used for CI and test environments

source 'https://rubygems.org'

ruby '3.0.5'

# Core dependencies for running tests and checking coverage
gem 'rspec'
gem 'simplecov', require: false
gem 'simplecov-json', require: false